# Package version tracking for Ridelines components
# This file is automatically updated by the version-updater workflow

production:
  hub:
    image: ghcr.io/ridelines/hub
    version: "sha-placeholder"  # Will be updated once packages are published
    
  drivetrain:
    lambda:
      image: ghcr.io/ridelines/drivetrain-lambda
      version: "sha-placeholder"  # Will be updated once packages are published
    tippecanoe_layer:
      image: ghcr.io/ridelines/drivetrain-tippecanoe-layer
      version: "sha-placeholder"  # Will be updated once packages are published

development:
  hub:
    image: ghcr.io/ridelines/hub
    version: "latest"
    
  drivetrain:
    lambda:
      image: ghcr.io/ridelines/drivetrain-lambda
      version: "latest"
    tippecanoe_layer:
      image: ghcr.io/ridelines/drivetrain-tippecanoe-layer
      version: "latest"