name: Stage Development Release

run-name: Stage dev - ${{ inputs.bundle_version }}

on:
  workflow_dispatch:
    inputs:
      bundle_version:
        description: 'Bundle version to stage for development'
        required: true
        type: string

permissions:
  contents: write
  packages: read
  id-token: write
  actions: write

jobs:
  stage:
    name: Stage Development Release
    uses: ./.github/workflows/base-stage.yml
    with:
      environment: dev
      bundle_version: ${{ inputs.bundle_version }}
    secrets: inherit